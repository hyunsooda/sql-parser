# sql-parser
Syntax & Token Parser

Use case 
1. It can do something that we make a transfiler like a Babel 
2. It can do something that we make a syntax parser which is fit to our purpose. 
3. It can do something that we make a simple flexible commands for user intraction

Conclusively, we can make an our syntax and use for specific purpose.
Regular expression can't analysis and parse the token string. it can just only recognize token, not syntax.


[How to Run]

0. yacc -d sql.y
1. lex sql.l
2. yacc sql.y
3. gcc y.tab.c -ll -ly
4. ./a.out

> ./a.out
> Enter the query: select id from tt;
> INPUT ACCEPTED....

