# sql-parser
Syntax &amp; Token Parser

Use case 
1. It can that we make a transfiler such as Babel 
2. It can that we make a syntax parser which is fitted to our purpose. 
3. It can that we make a simple flexible commands for user intraction

Conclusively, we can make a our syntax and use for something.
Regular expression can't analysis and parse the token string. it can just only recognize token, not syntax.
